machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 6.10.0

dependencies:
  override:
    - yarn --frozen-lockfile
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn test
