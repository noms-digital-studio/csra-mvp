machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - yarn --frozen-lockfile
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn test
