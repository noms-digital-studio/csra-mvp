
const defaultState = {
    current: {
        nomsID: "A5196FV"
    }
}

export default (state = defaultState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

