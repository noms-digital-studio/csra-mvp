
const defaultState = {
    current: {
        nomsID: "A1558YO"
    }
}

export default (state = defaultState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

